# Server Agent Notes

Follow root `AGENTS.md` in repo root.

Current status:
- Hono + Drizzle wired as the API runtime in `server/hono`.
- Cloudinary signed upload + webhook routes wired in Hono.

Next steps:
- MongoDB -> Postgres one-shot migration (see `MIGRATION_PLAN.md`).
